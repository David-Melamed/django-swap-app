apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swappod.fullname" . }}-test-connection"
  labels:
    {{- include "swappod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swappod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
