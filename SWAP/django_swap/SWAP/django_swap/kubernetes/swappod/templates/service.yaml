apiVersion: v1
kind: Service
metadata:
  name: {{ include "swappod.fullname" . }}
  labels:
    {{- include "swappod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swappod.selectorLabels" . | nindent 4 }}
